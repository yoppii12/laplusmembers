<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Bootstrap Sample</title>
        <!-- BootstrapのCSS読み込み -->
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <!-- jQuery読み込み -->
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
        <!-- BootstrapのJS読み込み -->
        <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
    </head>

    <body>
        <!-- ここから　-->
        <h1>
        <%= gravatar_for @user %>
    
        <%= @user.name %>
        <!-- ここまで　-->
        <div class="btn-group">
	        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
		        取引ステータス変更
		        <span class="caret"></span>
	        </button>
	        <ul class="dropdown-menu" role="menu">
		        <li role="presentation"><a role="menuitem" tabindex="-1" href="#"><%= button_to '通常取引', user_path(@user), method: :put, params: {status: '0'} %></a></li>
		        <li role="presentation"><%= button_to '売り停止', user_path(@user), method: :put, params: {status: '1'} %></li>
		        <li role="presentation"><%= button_to '売買停止', user_path(@user), method: :put, params: {status: '2'} %></li>
		        <li role="presentation"><%= button_to '全株放棄', user_path(@user), method: :put, params: {status: '3'} %></li>
	        </ul>
        </div>
        
        <%= "Stock Datas" %>
    
        </h1>
        <div class = "container">
            <div class = "table-responsive">
                <table class = "table table-bordered">
                    <thead>
                        <tr>
                            <th>コード</th>
                            <th>銘柄</th>
                            <th>株価</th>
                            <th>前日比</th>
                            <th>取引</th>
                            <th>取引単位</th>
                            <th>保有株数</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @datas.each do |data| %>
                            <tr>
                                <td><%= data.code %></td>
                                <td><%= data.bn %></td>
                                <td><%= data.sp %></td>
                                <td><%= data.dbr %></td>
                                <td><%= data.tr %></td>
                                <td><%= data.tru %></td>
                                <td><%= data.nsh %></td>
                            </tr>
                            
                        <% end %>
                    </tbody>
                </table>
            </div>
        </div>
    </body>
</html>